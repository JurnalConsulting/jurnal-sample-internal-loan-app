%br
- if @forms.blank?
  No forms
-else
  - @forms.each do |f|
    id:
    = link_to f.id, form_path(f)
    name:
    = f.form_name
    = link_to "delete", form_path(f),method: :delete, data: { confirm: "Are you sure you want to delete this post?" }
    %br
